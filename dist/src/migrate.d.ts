/**
 * Run migrations on the database, skipping the ones already applied. Also creates the sqlite db if it doesn't exist.
 */
export declare function runDbMigrations(): Promise<void>;
//# sourceMappingURL=migrate.d.ts.map